init()

InitializeAppData()

loadAllData(url)
- Ejecuta loadJSON a los archivos .json de la carpeta data.
- El resultado se guarda en la variable allData.
- 







loadJSON(url) (Funcion Util)
- Hace un fetch y lo guarda parseado en JSON en data.